[project]
name = "knowledge-rag-mcp"
version = "0.1.0"
description = "Knowledge RAG MCP server for finance domain"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]